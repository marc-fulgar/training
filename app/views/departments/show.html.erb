<p id="notice"><%= notice %></p>

<p>
	<br>
  <strong>Department name:</strong>
  <%= @department.name %>
	<br>
	<ul>
		<% @users.each do |user| %>
			<li><%= link_to "#{user.last_name}, #{user.first_name}", user_path(user.id)%>
		<% end %>
	</ul>
</p>